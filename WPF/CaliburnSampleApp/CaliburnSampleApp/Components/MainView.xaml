<UserControl x:Class="CaliburnSampleApp.Components.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CaliburnSampleApp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Label Content="Message:"/>
            <TextBox Margin="2" MinWidth="100" x:Name="Message" />
            <Button x:Name="ShowMessage" Content="Show Message" /> <!-- Bind directly to ShowMessage method -->
        </StackPanel>
        
        <GroupBox Grid.Row="1" Header="V1">
            <ContentControl x:Name="V1ViewModel" /> <!-- Use injected ViewModel to display entire UserControl -->
         </GroupBox>
        
    </Grid>
</UserControl>
